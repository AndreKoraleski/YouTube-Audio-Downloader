[project]
name = "yt-downloader"
version = "0.1.0"
description = "Download de vídeos do YouTube usando yt-dlp."
readme = "README.md"
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"